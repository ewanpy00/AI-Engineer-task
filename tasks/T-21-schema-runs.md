# T-21 — Схема: runs

Срез: S3. Обязательная часть: да.

## Цель

Добавить журнал заходов `runs` по DDL из `docs/10-design.md` §3.4 — источник
итоговых счётчиков захода и статус для страницы мониторинга (T-36, T-38).

## Файлы

- `schema.sql` — таблица `runs`, индекс `runs_started_idx`

## Зависимости

T-02.

## Определение готовности

- Таблица существует после применения schema.sql, `status` ограничен CHECK
  значениями `running/ok/empty/failed/skipped_locked`.

## Оценка

20 минут.
